<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{ shop.name }}</title>
  <link rel="stylesheet" href="{{ 'styles.css' | asset_url }}" />
</head>
<body style="background-color: {{ settings.color_bg }};">
  {{ content_for_layout }}
</body>
</html>
